const todo = require('./service/todo')
const loginAndJoin = require('./pages/login_and_join')
const user = require('./pages/user')

module.exports = {
  todo: todo,
  user: user,
  loginAndJoin: loginAndJoin
}
